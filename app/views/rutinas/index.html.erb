<h1>Listing rutinas</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Puntos</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rutinas.each do |rutina| %>
  <tr>
    <td><%= rutina.nombre %></td>
    <td><%= rutina.puntos %></td>
    <td><%= link_to 'Show', rutina %></td>
    <td><%= link_to 'Edit', edit_rutina_path(rutina) %></td>
    <td><%= link_to 'Destroy', rutina, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Rutina', new_rutina_path %>
